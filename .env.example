# ============================================================
# 环境变量模板
#
# 用法：复制这个文件为 .env，然后把值填上
#     Windows:   copy .env.example .env
#     Mac/Linux: cp .env.example .env
#
# 为什么用 .env 而不是 setx / export？
#   · setx 是 Windows 专有命令，服务器（Linux）上根本不存在 —— 部署时那套会失效
#   · setx 设完必须【重开终端】才生效，而且改的是系统级环境变量，卸载时容易忘
#   · .env 跟着项目走，clone 下来复制一份就能跑，三个平台一致
#
# .env 已被 .gitignore 挡住（见 .gitignore 里的 ".env" 和 ".env.*"），
# 所以真实密钥不会进 git。这个模板文件本身没有密钥，可以提交。
# ============================================================


# ------------------------------------------------------------
# 【必填】DeepSeek API Key
#   申请地址：https://platform.deepseek.com
#   取 key 的路径：控制台 → API keys → 创建
#   service.py 里的 ChatDeepSeek 会自动读这个变量，代码里不用显式传
#
#   ⚠️ 注意：不填这个，服务能启动、自检也能全绿（把关和检索不调模型），
#      但一旦有用户提问、真的要生成答案时就会抛 ValidationError。
# ------------------------------------------------------------
DEEPSEEK_API_KEY=sk-在这里填你的真实key


# ------------------------------------------------------------
# 【建议填】HuggingFace 镜像
#   国内直连 huggingface.co 会超时，第一次拉 embedding 模型（bge-small-zh，约 91MB）必须走镜像。
#   不填的话代码里有默认值 https://hf-mirror.com（见 service.py 的 setdefault），
#   所以留空也不会崩 —— 但写出来更清楚，别人一眼知道在走镜像。
# ------------------------------------------------------------
HF_ENDPOINT=https://hf-mirror.com


# ------------------------------------------------------------
# 【可选】embedding 模型缓存目录
#   默认放在当前用户主目录下的 ~/.cache/fastembed（Windows 即 C:\Users\<你>\.cache\fastembed）
#   模型约 91MB，第一次下载要几分钟，之后秒开。★ 别删，删了要重下。
#   只在想把缓存放到别处（比如空间大的 D 盘）时才需要填，路径用绝对路径。
# ------------------------------------------------------------
# FASTEMBED_CACHE_PATH=D:/cache/fastembed


# ------------------------------------------------------------
# 【可选】LangSmith 追踪
#   关掉（false）能显著提速：开着的话每个 LangGraph 步骤都会联网上报，慢的时候会卡住。
#   experiments/ 里的教学脚本尤其需要关掉它。
#   不填的话 tests/ 会自己设成 false（见 tests/test_guard.py 顶部）。
#   想用 LangSmith 看调用轨迹时才设 true，并且还需要 LANGCHAIN_API_KEY。
# ------------------------------------------------------------
LANGSMITH_TRACING=false
# LANGCHAIN_API_KEY=lsv2_在这里填你的LangSmith key