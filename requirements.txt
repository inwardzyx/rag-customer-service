# 客服知识库问答（RAG）依赖清单
# 版本为本机实测可跑的组合（Python 3.14 + Windows 11）

langgraph==1.2.11
langchain==1.4.0
langchain-core==1.6.3
langchain-deepseek==1.1.0
langchain-text-splitters==1.1.2

# 读 .env 文件（配密钥用，模板见 .env.example）
# 注：它本来是 langchain 的间接依赖，这里显式写出来 ——
#     直接 import 的东西就该直接声明，不能靠"别人会顺手装上"。
python-dotenv==1.2.3

# 中文分词（BM25 字面检索要用）
jieba==0.42.1
rank-bm25==0.2.2

# embedding 模型加载（bge-small-zh-v1.5）
# 注：fastembed 会依赖 mmh3；若本机应用控制策略拦掉 mmh3 的 DLL，
#     项目里的 env_compat.py 会用纯 Python 版兜底，不影响运行。
fastembed==0.8.1
onnxruntime==1.30.0
numpy==2.5.3

# HTTP 服务
fastapi==0.141.1
uvicorn==0.53.0
pydantic==2.13.5

# 测试（回归断言用，跑法见 README）
pytest==9.1.1

# 向量检索加速（本项目没装）：目前直接用 numpy 全量点乘，7 条语料完全够。
# 语料上千条之后才需要换成 faiss / HNSW。
# 注：本机试装 faiss 时它的 DLL 被应用控制策略拦了（和 mmh3 同一个坑），所以干脆没引入。
# faiss-cpu==1.13.0
